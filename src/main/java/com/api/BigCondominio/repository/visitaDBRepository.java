package com.api.BigCondominio.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.api.BigCondominio.model.visitaDB;

public interface visitaDBRepository extends JpaRepository<visitaDB, Long> {
}
